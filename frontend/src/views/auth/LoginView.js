import { render, init, destroy } from './Login.js';

var LoginView = {
  render: function (el) { render(el); },
  init: function () { init(); },
  destroy: function () { destroy(); }
};

export default LoginView;

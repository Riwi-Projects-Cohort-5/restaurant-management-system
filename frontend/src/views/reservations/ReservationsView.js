import { renderReservations } from './list.js';

var ReservationsView = {
  render: function (el) {
    renderReservations(el);
  },
  init: function () {},
  destroy: function () {}
};

export default ReservationsView;
